create table stadiums (
  stadium text,
  city text,
  state text,
  team text,
  conference text,
  capacity integer,
  record integer,
  built integer,
  expanded integer,
  place_gid integer,
  statefp text,
  placefp text,
  alt text
);
