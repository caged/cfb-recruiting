CREATE TABLE institutions (
  name text,
  stadium text,
  city text,
  state text,
  conference text,
  capacity integer,
  record integer,
  built integer,
  expanded integer,
  statefp integer,
  placefp integer,
  alt text
);
