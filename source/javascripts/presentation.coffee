# render = (section, height) ->
#   $(section).css 'height', height


# $ ->
#   height = $(window).height()
#   for section in $('.js-section')
#     render section, height

# $(document).on 'resize', '.section', (e) ->
#   console.log e
